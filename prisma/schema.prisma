generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_PRISMA_URL")
}


model Race {
  id        Int      @id @default(autoincrement())
  circuit   String
  cars      String
  maxir     Int

  @@map("races")
}
